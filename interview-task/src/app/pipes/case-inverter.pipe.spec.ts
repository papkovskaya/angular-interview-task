import { CaseInverterPipe } from './case-inverter.pipe';

describe('CaseInverterPipe', () => {
  it('create an instance', () => {
    const pipe = new CaseInverterPipe();
    expect(pipe).toBeTruthy();
  });
});
